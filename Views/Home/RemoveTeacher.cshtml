@using NTR20Z.Models

@model Reader 

@{
    ViewData["Title"] = "Planner";
}

<div class="text-center">
    <h1 class="display-4">Ekran usuwania nauczyciela</h1>
    
</div>

@using(Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "nie dziala", new{@class = "text-danger"})

    <div>
        @Html.LabelFor(model=>model.teachers)
        @Html.DropDownListFor(Model=>Model.chosenTeacher, new SelectList(Model.teachers), "Select teacher", new {htmlAtributes = new {@class = "form-control"}})
    </div>

    <button class="btn btn-danger" 
    onclick="location.href='@Url.Action("RemoveTeacher", "Home", new { id = Model.chosenTeacher})'">Usuń nauczyciela</button>
}